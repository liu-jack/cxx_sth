#pragma once

#include "def/TypeDef.h"

class Player;
class PlayerMarketLog;

class PlayerMarketLogDB {
public:
    static void SendCollectToDb(Player& player, PlayerMarketLog& log);
    static void SendBlackToDb(Player& player, PlayerMarketLog& log);
    static void SendBuyToDb(Player& player, PlayerMarketLog& log);
    static void SendMysteryToDb(Player& player, PlayerMarketLog& log);
};