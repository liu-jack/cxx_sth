#include "gtest/gtest.h"

#include "stage/StageManager.h"

//TEST( StageLevel, StageLevelSweepCost)
//{
    //const StageLevel* level = sStageManager.GetStageLevelById( 10001);

    //EXPECT_EQ( level->CalculateSweepCost( 0, 1) , 0);
    //EXPECT_EQ( level->CalculateSweepCost( 1, 1) , 0);
    //EXPECT_EQ( level->CalculateSweepCost( 2, 1) , 0);
    //EXPECT_EQ( level->CalculateSweepCost( 3, 1) , 0);
    //EXPECT_EQ( level->CalculateSweepCost( 4, 1) , 0);
    //EXPECT_EQ( level->CalculateSweepCost( 5, 1) , 0);
    //EXPECT_EQ( level->CalculateSweepCost( 9, 1) , 0);
    //uint32 cost = level->CalculateSweepCost( 10, 1);
    //EXPECT_EQ( level->CalculateSweepCost( 10, 5) , 5);


//}